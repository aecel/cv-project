import React, { Component } from "react"

class ExpSection extends Component {
  // constructor(props) {
  //   super(props)
  // }

  render() {
    const { info } = this.props

    return (
      <div>
        <h4>Hello, I am an ExpSection</h4>
      </div>
    )
  }
}

export default ExpSection
