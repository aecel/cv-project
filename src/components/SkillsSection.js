import React, { Component } from "react"

class SkillsSection extends Component {
  // constructor(props) {
  //   super(props)
  // }

  render() {
    const { info } = this.props

    return (
      <div>
        <h4>Hello, I am a SkillsSection</h4>
      </div>
    )
  }
}

export default SkillsSection
